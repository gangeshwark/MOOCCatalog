<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <style name="AppTheme.Tunefit" parent="AppTheme.Theme">
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:windowContentTransitions">true</item>
        <item name="android:windowAllowEnterTransitionOverlap">true</item>
        <item name="android:windowAllowReturnTransitionOverlap">true</item>
        <item name="android:windowSharedElementEnterTransition">@android:transition/move</item>
        <item name="android:windowSharedElementExitTransition">@android:transition/move</item>

        <!-- colorPrimary is used for Notification icon and bottom facet bar icons
                and overflow actions -->
        <item name="android:colorPrimary">@color/primaryColor</item>

        <!-- colorPrimaryDark is used for background -->
        <item name="android:colorPrimaryDark">@color/primaryColorDark</item>

        <!-- colorAccent is sparingly used for accents, like floating action button highlight,
        progress on playbar-->
        <item name="android:colorAccent">@color/colorAccent</item>
        <item name="android:colorPressedHighlight">@color/textIcons</item>
        <item name="android:colorEdgeEffect">@color/textIcons</item>
        <item name="android:windowTranslucentStatus">false</item>
        <item name="android:divider">@color/dividerColor</item>
        <item name="android:textColorPrimary">@color/primaryText</item>
        <item name="android:textColorSecondary">@color/secondaryText</item>

        <item name="android:colorControlHighlight">@color/textIcons</item>
       <!-- <item name="android:windowBackground">@android:color/white</item>-->


        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>

        <item name="windowActionBar">false</item>

    </style>
    <style name="AppTheme.Tunefit.Trans" parent="AppTheme.Theme">
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowContentTransitions">true</item>
        <item name="android:windowAllowEnterTransitionOverlap">true</item>
        <item name="android:windowAllowReturnTransitionOverlap">true</item>
        <item name="android:windowSharedElementEnterTransition">@android:transition/move</item>
        <item name="android:windowSharedElementExitTransition">@android:transition/move</item>

        <!-- colorPrimary is used for Notification icon and bottom facet bar icons
                and overflow actions -->
        <item name="android:colorPrimary">@color/primaryColor</item>

        <!-- colorPrimaryDark is used for background -->
        <item name="android:colorPrimaryDark">@color/primaryColorDark</item>

        <!-- colorAccent is sparingly used for accents, like floating action button highlight,
        progress on playbar-->
        <item name="android:colorAccent">@color/colorAccent</item>
        <item name="android:colorPressedHighlight">@color/textIcons</item>
        <item name="android:colorEdgeEffect">@color/textIcons</item>
        <item name="android:windowTranslucentStatus">false</item>
        <item name="android:divider">@color/dividerColor</item>
        <item name="android:textColorPrimary">@color/primaryText</item>
        <item name="android:textColorSecondary">@color/secondaryText</item>

        <item name="android:colorControlHighlight">@color/textIcons</item>
        <!-- <item name="android:windowBackground">@android:color/white</item>-->


        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>

        <item name="windowActionBar">false</item>

    </style>

    <style name="AppTheme.Base.Dark" parent="AppTheme.Theme">
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowContentTransitions">true</item>
        <item name="android:windowAllowEnterTransitionOverlap">true</item>
        <item name="android:windowAllowReturnTransitionOverlap">true</item>
        <item name="android:windowSharedElementEnterTransition">@android:transition/move</item>
        <item name="android:windowSharedElementExitTransition">@android:transition/move</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>

        <item name="windowActionBar">false</item>

    </style>

</resources>
